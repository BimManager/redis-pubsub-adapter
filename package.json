{
  "name": "redis-pubsub-adapter",
  "version": "1.0.0",
  "description": "A simple implementation of an adapter that listens to one channel, processes the incoming messages and sends the results to another channel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "test": "npx tsc -p tsconfig.test.json && node ./dist/tests/integrationTests.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bimmanager/redis-pubsub-adapter.git"
  },
  "bugs": {
    "url": "https://github.com/bimmanager/redis-pubsub-adapter/issues"
  },
  "author": "kkozlov",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.45",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "redis": "^4.1.0"
  }
}
